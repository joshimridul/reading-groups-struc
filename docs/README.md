# Documentation

This folder contains project documentation beyond the main replication
instructions in the repository-level `README.md`.

Most collaborators can start with:

- `../README.md` for the paper, software, and replication workflow.
- `../replication_audit/PIPELINE_MAP.md` for a map of the analysis pipeline.
- `../replication_audit/REPLICATION_AUDIT.md` for the current audit notes.

Author-side workflow notes are kept in `internal_workflow/` so they do not
clutter the main repository surface.
